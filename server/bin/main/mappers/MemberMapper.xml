<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.management.mapper.user.MemberMapper">
	
	<select id="idCheck" parameterType="hashmap" resultType="Integer">
		SELECT ifnull(SUM(member_no),0) as member_no FROM member_info WHERE user_id=#{userId}
	</select>
	
	<select id="emailCheck" parameterType="hashmap" resultType="Integer">
		SELECT ifnull(SUM(member_no),0) as member_no FROM member_info WHERE email=#{email}
	</select>
	
	<select id="pwCheck" parameterType="hashmap" resultType="String">
		SELECT passwd FROM member_info 
		WHERE 1=1 AND
		<if test="userId != null and !userId.equals('')">
			user_id=#{userId}
		</if>
		<if test="email != null and !email.equals('')">
			email=#{email}
		</if>
	</select>
	
	<select id="loginCheck" parameterType="com.management.domain.model.Member" resultType="Boolean">
		SELECT IF(SUM(member_no) > 0 ,'true', 'false') as name FROM member_info 
		WHERE 1=1 AND
		<if test="userId != null and !userId.equals('')">
			user_id=#{userId}
		</if>
		<if test="email != null and !email.equals('')">
			email=#{email}
		</if>
		AND passwd=#{passwd}
	</select>
	
	<select id="viewMember" parameterType="hashmap" resultType="String">
		SELECT name FROM member_info
		WHERE 1=1 AND
		<if test="userId != null and !userId.equals('')">
			user_id=#{userId}
		</if>
		<if test="email != null and !email.equals('')">
			email=#{email}
		</if>
	</select>
</mapper>
